version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: faculty_tracker_db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password123
      MYSQL_DATABASE: faculty_tracker
      MYSQL_USER: user
      MYSQL_PASSWORD: password123
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always

volumes:
  mysql_data:
